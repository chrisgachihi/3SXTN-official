//package com.example.bottom.Database
/*
import androidx.lifecycle.LiveData
import com.example.bottom.Models.Drill

class DrillsRepository(private val drillDao: DrillDao) {

    val allDrills :LiveData<List<Drill>> = drillDao.getAllDrills()

    suspend fun insert(drill: Drill){

        drillDao.insert(drill)

    }
    suspend fun delete(drill: Drill) {

        drillDao.delete(drill)

    }
    suspend fun  update(drill: Drill){

        drillDao.update(drill.id,drill.title,drill.date)
    }
}
 */